// Re-export do client gerado pelo Lovable Cloud.
// Use `import { supabase } from "@/lib/supabase"` em novos arquivos.
export { supabase } from "@/integrations/supabase/client";
export type { Database } from "@/integrations/supabase/types";

// Atualiza o feriados.ts para usar cast na chamada (helper para evitar repetir)
